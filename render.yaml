services:
  - type: worker
    name: lopburi-filter-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: SOURCE_CHAT_ID
        sync: false
      - key: TARGET_CHAT_ID
        sync: false
